- name: Update HAProxy configuration
  template:
    src: haproxy.cfg.j2
    dest: /etc/haproxy/haproxy.cfg
  notify:
    - Restart HAProxy
  become: true

- name: Restart HAProxy
  service:
    name: haproxy
    state: restarted
  become: true
